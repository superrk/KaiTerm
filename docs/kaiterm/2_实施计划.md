# KaiTerm 实施计划 — 未完成功能跟踪

## 构建状态
- Rust 后端 ✅ 编译通过
- Vue 前端 ✅ 构建通过 (0 errors)

---

## 未实现 / 部分实现功能清单

### US-001: 连接管理
- [x] SSH Agent 认证后端实现
- [x] Windows DPAPI 加密（替换 XOR+Base64）
- [x] 连接超时检测 (15s) + 友好提示
- [x] 端口号范围校验 (1-65535)
- [x] 认证失败/主机不可达/超时的区分提示
- [x] 前端文件选择器（上传/下载用系统原生对话框）

### US-004: 目录跟随
- [x] 跟随开关（Toggle）
- [x] `cd` 命令检测（后端 set_sync_dir 推送当前目录）
- [x] SFTP 面板自动切换目录

### US-005: 多 Tab 管理
- [x] 动态 Tab 增删（每个 Tab 独立 SSH 会话）
- [x] 每 Tab 独立 xterm 实例和 SFTP 面板跟随
- [x] 独立 keepalive 保活
- [x] Tab 切换时 SFTP 面板联动
- [x] 关闭 Tab 断开对应连接
- [x] 10+ Tab 性能

### US-006: 本地终端
- [x] Shell 崩溃自动重启

### US-007: 文件传输
- [x] 文件冲突处理覆盖/跳过/重命名弹窗
- [x] 系统文件选择器（替换 prompt()）
- [x] 大文件 (>1GB) 验证 — 上传改为流式分块读取（不再整文件读入内存）+ 失败/取消清理半截文件；下载本就流式有界通道 + 失败清理

### US-008: 本地文件浏览器
- [x] 本地终端模式时面板切换到本地文件浏览
- [x] 本地文件目录导航前端集成

### 其他
- [ ] 断开重连按钮
- [ ] SFTP 按列排序逻辑
- [ ] xterm.js scrollback 配置
- [ ] 错误信息细化

---

## 分阶段实施计划

### Phase 0 — Quick Wins
1. SSH Agent 认证 → `ssh_session.rs:create_session` (AgentClient + openssh-ssh-agent 管道) — ✅ 已完成
2. DPAPI 加密 → `crypto.rs` (CryptProtectData/CryptUnprotectData + LocalFree) — ✅ 已完成（XOR 仅作解密兜底兼容）
3. 文件冲突弹窗 → `transfers.rs` (resolve_conflict + ConflictResolution) + `ConflictDialog.vue` — ✅ 已完成（含会话级"全部应用"覆盖）
4. 端口校验 (1-65535) + 连接超时 (15s) + 差异化错误 → `ConnectionModal.vue` + `ssh_session.rs:create_session` — ✅ 已完成
5. **连接配置去重** ✅ — `saveProfile` 按 host+port+user 指纹匹配；`loadProfiles` 过滤已存的重复项；`ConnectionModal` 断开引用、跟踪编辑目标

### Phase 1 ✅ — 核心架构改造（已完成）
5. **SFTP session 持久化** ✅
   - `SshSession` 持有 `Arc<Mutex<Option<SftpSession>>>`，`connect_ssh` 时初始化一次
   - `sftp_ops.rs`: 快速操作使用持久化 session；`new_sftp_channel()` 为传输创建独立通道
   - 影响文件: `ssh_session.rs`, `sftp_ops.rs`, `transfers.rs`
6. **多 Tab 管理** ✅
   - 动态 Tab 增删，每 Tab 独立 xterm 实例（div 容器切换）
   - 独立 keepalive（每连接 30s 间隔，max 3 次）
   - SFTP 每会话独立路径状态
   - Tab 关闭 → 销毁 terminal → 断开 SSH → 清理 SFTP
   - 影响文件: `App.vue`, `TerminalPanel.vue`, `sftp.js`, `connection.js`, `ssh_session.rs`

### Phase 2 — 智能联动
7. **目录跟随** ✅ — `connection.js` (syncTerminalDir 开关 + set_sync_dir) + `SftpPanel.vue` 监听后端目录事件
8. **本地文件浏览器集成** ✅ — `LocalFilePanel.vue` + `App.vue` 切换

### Phase 3 — 体验打磨
9. 重连按钮 + xterm scrollback 配置
10. 错误信息细化（超时/认证失败/不可达区分）— ✅ 已完成（与 Phase 0 #4 合并实现）
11. SFTP 按列排序

---

## 完成度汇总
- US-001 连接管理 ✅ 全部完成（Agent / DPAPI / 超时 / 端口校验 / 差异错误 / 原生选择器）
- US-004 目录跟随 ✅ 完成
- US-005 多 Tab ✅ 完成
- US-006 本地终端 ✅ 完成（Shell 崩溃自动重启，最多 5 次）
- US-007 文件传输 ✅ 完成（冲突弹窗 / 原生选择器 / >1GB 流式 + 残文件清理）
- US-008 本地文件浏览器 ✅ 完成
- 剩余：重连按钮、xterm scrollback、SFTP 排序（体验打磨，非 P0）

---

## 第一阶段启动
从 Phase 0 并行收 quick wins，或直接进入 Phase 1 架构改造（SFTP 持久化 → 多 Tab）。
